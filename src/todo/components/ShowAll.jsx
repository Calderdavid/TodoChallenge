import React, { useState, useEffect } from 'react'
import { Grid, 
    GridItem, 
    TabList, Tabs, Text, 
    Tab, Box, Checkbox, 
    TabPanels, TabPanel, Input, FormControl,
    Button, Stack, CheckboxGroup
} from '@chakra-ui/react'
import { useCheckbox } from '@chakra-ui/react'

export const ShowAll = ( props ) => {
    
    const tasks = [...props.tasks];    
    
    return (
        <>
   
        </>
    )
}
